import 'package:flutter/material.dart';

/// Abstract class [Header] for all Headers to be built from
abstract class Header extends StatelessWidget implements PreferredSizeWidget {
  const Header({super.key});
}
